import { createStyles } from "@mantine/core";

const useStyles = createStyles((theme) => ({
  alignCenter: {
    textAlign: "center",
  },

  innerLayout: {
    margin: "1rem",
    marginTop: "5rem",
    marginBottom: "2rem",
  },
}));

export default useStyles;
